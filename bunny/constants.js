
export const screen = {
  width: window.innerWidth,
  height: window.innerHeight,
  dpi: window.devicePixelRatio
}

export const physics = {
  gravity: 0.75,
  friction: -0.85
}

export const game = {
  toAdd: 1000
}
